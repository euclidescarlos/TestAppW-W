<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    card_view:cardCornerRadius="12dp"
    card_view:cardElevation="2dp">

    <LinearLayout
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:background="@drawable/vinho_card_background"
        android:orientation="vertical"
        android:padding="8dp">

        <ImageView
            android:id="@+id/imgVinho"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:scaleType="centerCrop"
            android:src="@drawable/placeholder_vinho" />

        <TextView
            android:id="@+id/txtNomeVinho"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Nome do Vinho"
            android:textSize="14sp"
            android:textStyle="bold"
            android:paddingTop="6dp" />

        <TextView
            android:id="@+id/txtPreco"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="R$ 00,00"
            android:textSize="13sp"
            android:textColor="#000000"
            android:paddingTop="2dp" />

        <ImageButton
            android:id="@+id/btnAdicionar"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="end"
            android:background="@drawable/btn_circle"
            android:src="@drawable/ic_add"
            android:tint="@android:color/white"
            android:contentDescription="Adicionar ao carrinho" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
